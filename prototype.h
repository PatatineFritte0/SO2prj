#ifndef PROTOTYPE_H
#define PROTOTYPE_H

//qui sono presenti tutti i prototipi di funzione presenti in function.h

char* readFile(const char*);

int getNqbit (char*);

bool isPresent(const char*, const char*, int*);
#endif //PROTOTYPE_H
